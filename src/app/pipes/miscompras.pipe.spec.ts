import { MiscomprasPipe } from './miscompras.pipe';

describe('MiscomprasPipe', () => {
  it('create an instance', () => {
    const pipe = new MiscomprasPipe();
    expect(pipe).toBeTruthy();
  });
});
